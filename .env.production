# Production Environment Configuration
# IMPORTANT: Update all values marked with "CHANGE-ME" before deployment!

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=coins2025
# NOTE: In Cloud Run, credentials are automatic - remove this line in production:
# GOOGLE_APPLICATION_CREDENTIALS=./credentials/service_account.json

# BigQuery Configuration
BQ_DATASET=db
BQ_TABLE=catalog
BQ_HISTORY_TABLE=history
BQ_GROUPS_TABLE=groups
BQ_GROUP_USERS_TABLE=group_users

# Application Settings
APP_ENV=production
LOG_LEVEL=WARNING
CACHE_DURATION_MINUTES=15

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Security Settings (Production - Strict)
ADMIN_API_KEY=CHANGE-ME-super-secret-admin-key-min-32-chars
ADMIN_ALLOWED_IPS=CHANGE-ME-admin-workstation-ip
ENABLE_ADMIN_ENDPOINTS=false
ENABLE_OWNERSHIP_ENDPOINTS=false
ENABLE_DOCS=false
REQUIRE_ADMIN_AUTH=true
STRICT_CORS=true

# Production Notes:
# 1. Set ADMIN_API_KEY to a strong, random 32+ character string
# 2. Set ADMIN_ALLOWED_IPS to specific IP addresses of admin workstations
# 3. Admin endpoints are DISABLED by default for maximum security
# 4. To enable admin access, set ENABLE_ADMIN_ENDPOINTS=true and ensure proper auth
# 5. API documentation is DISABLED in production